# attest-control-plane

**Name reserved. Not yet released.**

A governed control plane for AI actions: verifiable warrants, bound authorization, and
provenance-sealed attestations.

This placeholder holds the distribution name while the project is developed in the open.
It installs no modules and does nothing — do not depend on it.

To use the package today, install from a pinned commit:

```bash
pip install "attest-control-plane @ git+https://github.com/Threx-code/attest.git@<sha>"
```

Source, documentation and issues: **https://github.com/Threx-code/attest**
