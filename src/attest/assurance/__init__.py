"""L3 — assurance. Proving it works.

The conformance kit a domain package runs against its own profile, the ten red-team
families, the evaluation harness, and offline-verifiable evidence bundles.

The conformance kit is the load-bearing piece: a domain profile is written by people
we never meet, in a repo we never see, so shipping the test is the only way the
openness claim is testable rather than asserted.
"""

from __future__ import annotations

__all__: list[str] = []
